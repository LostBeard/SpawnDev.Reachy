bin/
obj/
*.user

.vs/

# Kokoro downloads its 310MB model into the working directory on first run.
kokoro.onnx

# Pre-extracted show audio + captions for voice cloning (~338MB).
# Private home use only - never distribute audio impersonating the real voice actors.
scratchpad/
md_audio/
*.wav
*.srt

# ASR + VAD models, fetched separately (~250MB)
models/

# GPU runtime: cuDNN 9 for CUDA 13 (~540MB) + its installer (~1.2GB).
# Reproduce with gpu-setup/GPU-SETUP.md; never commit the binaries.
SpawnDev.Reachy.Rose/gpu-runtime/
gpu-setup/*.zip
